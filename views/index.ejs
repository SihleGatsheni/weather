<!DOCTYPE html>
<html>
<head>
	<title>Weather App</title>
	<link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<div class = 'container'>
	<header>
		<div class="compass-icon">
			<img src="/icons/" alt="icon">
		</div>
	</header>
	<div class ="weather-title">
		<h1>CG Weather</h1>
		<p>
		View the current weather for your City
	
	</div>
	<form action="/" method="POST">
		<input type="text" name="city" class="ghost-input" placeholder="Enter City">
	</form>
	<div class="result">
		<% if(temp !==null) {%>
			<h1 class="temp"><%= temp%></h1>
		<%} %>
		
		<div class="icons-des">
			<% if(icon !==null) {%>
				<img class ="icon" src="http://openweathermap.org/img/w/<%= icon%>.png" alt="icon">
			
		<%} %>
			
			<% if(des !==null) {%>
			<h3><%= des %></h3>
		<%} %>

		</div>
		<% if(temp !==null) {%>
			<p class="city"><%= city%></p>
		<%} %>
		
	</div>
</div>
</body>
</html>